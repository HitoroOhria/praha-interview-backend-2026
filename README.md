# Paraha Interview Backend 2026

## Development

### Setup

```shell
$ docker compose up -d
```

### Run on local

以下の環境変数を指定してください。

- `DATABASE_URL`
- `TEST_DATABASE_URL`

Docker Compose 経由で実行すると、自動的に設定されます。Task コマンドを使用することにより、楽に Docker Compose のコマンドを実行で聴きます。
